apiVersion: v1
kind: Service
metadata:
  annotations:
    metallb.io/loadBalancerIPs: 192.168.0.122
  labels:
    io.kompose.service: littlelink-server
  name: littlelink-server
  namespace: littlelink
spec:
  type: LoadBalancer
  ports:
    - name: "8080"
      port: 8080
      targetPort: 3000
  selector:
    app: littlelink-server
